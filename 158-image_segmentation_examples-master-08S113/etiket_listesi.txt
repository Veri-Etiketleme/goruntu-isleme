Python
    os
        path
            join
            exists
    cv2
        imread
        VideoCapture
        VideoWriter_fourcc
        waitKey
        destroyAllWindows
    mrcnn
        config
        visualize
            display_instances
            apply_mask
        utils
            download_trained_weights
        model
            MaskRCNN
            modellib
            utils 
    pathlib
        Path
    numpy(np)
        zeros
        uint8
        int32
        ones
        bool
    matplotlib
        pyplot(plt)
            imshow
            show
    warnings
        catch_warnings
        simplefilter
    xmltodict
        parse
    skimage
        draw
Functions
    import
    return
    continue
    from
    if
    for 
    in 
    while
    as 
    break
    with 

    XML